
/*
angular.module('myApp', ['ngRoute'])
  .config(['$routeProvider', ($routeProvider) ->

    $routeProvider
    .when '/main', {
      controller : 'mainCtrl'
      templateUrl : 'partials/main.html'
    }
    .otherwise {
      redirectTo: '/main'
    }
  ])
 */

(function() {
  var counter;

  counter = function() {};

}).call(this);
